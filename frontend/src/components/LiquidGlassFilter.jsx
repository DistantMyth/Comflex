/**
 * LiquidGlassFilter.jsx
 * Safe no-op stub: all components now use hardware-accelerated CSS backdrop filters
 * (blur + saturation) to avoid software rasterization and GPU memory leaks in Chromium.
 */

export default function LiquidGlassFilter() {
  return null;
}
